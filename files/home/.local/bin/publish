#!/bin/bash

# Add here tests from git pre-commit hook
# or pipenv run test

# use -r arg for twine repository select

if [[ -f setup.py ]]; then
    python3 setup.py bdist_wheel && twine upload dist/*; rm dist/*
else
    echo "No setup.py file"
fi
